---
phase: 2.1
slug: optimize-grader-model-selection
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-09
---

# Phase 2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Custom ts-node test runner (assert + process.exit) + vitest for unit tests |
| **Config file** | vitest.config.ts (unit), none for benchmark |
| **Quick run command** | `npm run test:ollama-grader` |
| **Full suite command** | `npm run test:bootstrap` |
| **Estimated runtime** | ~5s (unit), ~60s (e2e) |

---

## Sampling Rate

- **After every task commit:** Run `npm run test:ollama-grader`
- **After every plan wave:** Run `npm run test:bootstrap`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | SC-1: Valid 0.0-1.0 scores within 60s | benchmark | `npx ts-node tests/benchmark-grader.ts` | W0 | pending |
| TBD | TBD | TBD | SC-2: Direct Ollama API verification on both platforms | benchmark + CI | `npx ts-node tests/benchmark-grader.ts` + CI workflow | W0 | pending |
| TBD | TBD | TBD | SC-3: e2e bootstrap test passes with llm_rubric > 0.0 | e2e | `npm run test:bootstrap` | Exists | pending |
| TBD | TBD | TBD | SC-4: Ollama env tuning validated on both platforms | benchmark | `bash scripts/ollama-bench.sh` + CI workflow | W0 | pending |

*Status: pending · green · red · flaky*

---

## Wave 0 Requirements

- [ ] `tests/benchmark-grader.ts` — benchmark script (core deliverable)
- [ ] `tests/fixtures/benchmark/session-positive.json` — captured session log from test:bootstrap
- [ ] `tests/fixtures/benchmark/session-empty.json` — synthetic negative case (empty agent output)
- [ ] `tests/fixtures/benchmark/session-wrong.json` — synthetic negative case (wrong solution)
- [ ] `scripts/ollama-bench.sh` — wrapper script for Ollama lifecycle + profile orchestration
- [ ] `.github/workflows/benchmark-grader.yml` — CI benchmark workflow

*Existing infrastructure: vitest for unit tests (`npm run test:ollama-grader`), e2e bootstrap test (`npm run test:bootstrap`)*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Model scoring plausibility | SC-1 | Requires human judgment on whether scores are reasonable for the test cases | Review BENCHMARK.md scores; verify positive case > negative cases |
| CI vs local result comparison | SC-2 | Requires interpreting performance differences across platforms | Compare CI artifact results with local BENCHMARK.md; document in report |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
