---
phase: 02-local-llm-grader
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/bootstrap.test.ts
autonomous: false
requirements: [GRADE-08]

must_haves:
  truths:
    - "Deterministic grader still scores 1.0 on the superlint task after Ollama integration"
    - "Existing bootstrap test passes without modification"
    - "LLM grading does not interfere with deterministic grading when Ollama is unavailable"
  artifacts:
    - path: "tests/bootstrap.test.ts"
      provides: "Existing integration test unchanged and passing"
  key_links:
    - from: "src/graders/index.ts (getGrader)"
      to: "DeterministicGrader"
      via: "type='deterministic' dispatch unchanged"
      pattern: "case 'deterministic'"
    - from: "src/graders/index.ts (LLMGrader.grade)"
      to: "score 0 fallthrough"
      via: "Ollama unavailable + no cloud keys = score 0, does not affect deterministic grader"
---

<objective>
Verify that the Ollama integration from Plan 01 does not break existing deterministic grading.

Purpose: GRADE-08 requires the deterministic grader to still score 1.0 on the superlint task. This plan runs the existing bootstrap test to confirm backward compatibility, then provides a checkpoint for the user to optionally verify with a real Ollama instance.

Output: Confirmed regression-free integration; user-verified with real Ollama if available.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-local-llm-grader/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: the Ollama integration is in LLMGrader only. -->
<!-- DeterministicGrader and getGrader() are unchanged. -->
<!-- The bootstrap test runs the full eval pipeline with both graders. -->

From src/graders/index.ts (unchanged):
```typescript
export class DeterministicGrader implements Grader {
    // Runs shell command, scores based on exit code + reward.txt
    // NOT modified by Plan 01
}

export function getGrader(type: string): Grader {
    switch (type) {
        case 'deterministic': return new DeterministicGrader();
        case 'llm_rubric': return new LLMGrader();
        // NOT modified by Plan 01
    }
}
```

From tests/bootstrap.test.ts:
```typescript
// Runs full eval pipeline with a solving agent
// Pass criteria: report.pass_rate >= 0.5 (deterministic weight 0.7 alone is sufficient)
// LLM grader returns score 0 when no API keys set (now also when no Ollama)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run regression tests to confirm deterministic grader unaffected</name>
  <files>tests/bootstrap.test.ts</files>
  <action>
Run the existing bootstrap test suite to confirm the deterministic grader still scores 1.0 on the superlint task and the overall pass_rate is still >= 0.5.

1. Run `npm run test:bootstrap` -- this exercises the full eval pipeline including both the deterministic grader (weight 0.7) and the LLM grader (weight 0.3)
2. The deterministic grader must still score 1.0 (it runs `bash tests/test.sh` which is unaffected)
3. The LLM grader will return score 0 (Ollama not running + no cloud API keys in test env) -- this is expected and correct behavior (OLLAMA-03 graceful degradation)
4. Overall pass_rate will be ~0.7 (1.0 * 0.7 + 0.0 * 0.3 = 0.7), which is >= 0.5 threshold

Also run the Ollama mock tests from Plan 01 to confirm they still pass:
5. Run `npm run test:ollama-grader`

Also run the analytics test:
6. Run `npm run test:analytics`

If any test fails, diagnose and fix. The most likely issue would be if the Plan 01 changes accidentally broke the grade() method signature or the DeterministicGrader class. If so, fix the regression in `src/graders/index.ts` while preserving the Ollama integration.

Do NOT modify `tests/bootstrap.test.ts` -- the existing test should pass as-is. The point is to prove backward compatibility.
  </action>
  <verify>
    <automated>npm run test:bootstrap && npm run test:ollama-grader && npm run test:analytics</automated>
  </verify>
  <done>
    - `npm run test:bootstrap` passes -- deterministic grader scores 1.0, overall pass_rate >= 0.5
    - `npm run test:ollama-grader` passes -- all Ollama mock tests green
    - `npm run test:analytics` passes -- analytics pipeline unaffected
    - No modifications to test files needed (backward compatible)
  </done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: User verifies Ollama grading with real instance</name>
  <files>src/graders/index.ts</files>
  <action>
Ollama-backed LLM grading has been integrated into the existing LLMGrader fallback chain. When Ollama is running with the required model pulled, grading uses the local model. When Ollama is absent, grading falls through to cloud providers (if API keys set) or returns score 0 with an actionable error.

The user should verify one of the following scenarios depending on their setup:

**If Ollama is installed and running:**
1. Ensure Ollama is running: `ollama serve` (or check `curl http://localhost:11434/`)
2. Pull the default model: `ollama pull qwen3:4b`
3. Run the evaluation: `npm run eval:superlint`
4. Expected: The LLM grader produces a 0.0-1.0 score using the local Ollama model. Look for `llm_rubric` with a non-zero score.

**If Ollama is NOT installed:**
1. Run: `npm run eval:superlint`
2. Expected: Deterministic grader scores 1.0, LLM grader scores 0 with "Ollama is not running" message. Overall pass_rate ~0.7.

**Error message verification (optional):**
- Ollama stopped + no cloud keys: LLM grader shows "Ollama is not running at http://localhost:11434. Start it with: ollama serve"
- Ollama running but model not pulled: LLM grader shows 'model "qwen3:4b" is not pulled. Run: ollama pull qwen3:4b'
  </action>
  <verify>User confirms expected behavior based on their Ollama availability.</verify>
  <done>User has verified that Ollama grading works correctly (or degrades gracefully) in their environment.</done>
</task>

</tasks>

<verification>
1. `npm run test:bootstrap` -- existing test passes, deterministic grader scores 1.0
2. `npm run test:ollama-grader` -- all Ollama mock tests pass
3. `npm run test:analytics` -- analytics pipeline unaffected
</verification>

<success_criteria>
- All three test suites pass: bootstrap, ollama-grader, analytics
- Deterministic grader scores 1.0 on superlint task (GRADE-08 confirmed)
- No modifications to existing test files required
- User optionally verifies with real Ollama instance
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-llm-grader/02-02-SUMMARY.md`
</output>
