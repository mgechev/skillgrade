---
phase: 02.1-optimize-grader-model-selection
plan: 01
subsystem: testing
tags: [ollama, benchmark, typescript, shell, arm64, grader-model]

# Dependency graph
requires:
  - phase: 02-local-llm-grader
    provides: LLMGrader with callOllama, prompt construction pattern, LogEntry types
provides:
  - Standalone benchmark script (tests/benchmark-grader.ts) for evaluating grader models via direct /api/generate
  - Three test fixtures for benchmark evaluation (positive, empty, wrong sessions)
  - Cross-platform Ollama lifecycle wrapper (scripts/ollama-bench.sh) for three-profile tuning matrix
affects: [02.1-02, 02.1-03, 02.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns: [direct-ollama-fetch-benchmark, json-schema-structured-output, three-point-tuning-matrix]

key-files:
  created:
    - tests/benchmark-grader.ts
    - tests/fixtures/benchmark/session-positive.json
    - tests/fixtures/benchmark/session-empty.json
    - tests/fixtures/benchmark/session-wrong.json
    - scripts/ollama-bench.sh
  modified: []

key-decisions:
  - "Synthetic positive fixture created instead of captured from live run -- Ollama not running during plan execution"
  - "Benchmark script copies prompt construction from LLMGrader rather than importing -- standalone by design"
  - "JSON Schema structured output as default format with no-schema fallback on failure"
  - "120s hard timeout per /api/generate call via AbortSignal.timeout"
  - "CPU cores detected at runtime via os.cpus().length for num_thread in optimized-all profile"

patterns-established:
  - "Direct Ollama /api/generate benchmark pattern: fetch with JSON Schema format, warm-up, timing extraction"
  - "Cross-platform Ollama lifecycle: $OSTYPE detection, taskkill.exe on Windows, pkill on Linux"
  - "Three-point tuning matrix: default/default, optimized-env/default-api, optimized-env/optimized-api"

requirements-completed: [SC-1, SC-2, SC-4]

# Metrics
duration: 4min
completed: 2026-03-09
---

# Phase 2.1 Plan 01: Benchmark Harness Summary

**Standalone TypeScript benchmark script with 8 model-quant candidates, JSON Schema structured output, and cross-platform Ollama lifecycle wrapper for three-profile tuning matrix**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-09T11:30:25Z
- **Completed:** 2026-03-09T11:34:44Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Created standalone benchmark script (760 lines) that calls Ollama /api/generate directly with JSON Schema format, warm-up, model pulling, timing extraction, pass/fail evaluation, and console table output
- Created three test fixtures: realistic positive session (check/fix/verify workflow), empty-output negative case, and wrong-approach negative case
- Created cross-platform Ollama lifecycle wrapper (215 lines) that orchestrates three tuning profiles with platform-specific process management

## Task Commits

Each task was committed atomically:

1. **Task 1: Create benchmark test fixtures and TypeScript benchmark script** - `22777c3` (feat)
2. **Task 2: Create Ollama lifecycle wrapper shell script** - `e027616` (feat)

## Files Created/Modified
- `tests/benchmark-grader.ts` - Standalone benchmark script: CLI parsing, prompt construction, direct Ollama /api/generate calls, JSON Schema mode with fallback, results table, pass/fail evaluation
- `tests/fixtures/benchmark/session-positive.json` - Realistic positive session: agent runs superlint check/fix/verify workflow correctly
- `tests/fixtures/benchmark/session-empty.json` - Negative case: agent produced no output, ran no commands
- `tests/fixtures/benchmark/session-wrong.json` - Negative case: agent used sed instead of superlint, skipped mandatory workflow
- `scripts/ollama-bench.sh` - Cross-platform Ollama lifecycle wrapper: platform detection, start/stop/wait functions, three-profile orchestration, EXIT trap

## Decisions Made
- Constructed synthetic positive fixture instead of capturing from live run (Ollama not running); mirrors realistic test:bootstrap output with superlint check/fix/verify commands
- Benchmark script is fully standalone -- copies prompt construction pattern from LLMGrader rather than importing, per the plan's design decision
- Uses JSON Schema object as default `format` field; falls back to no-schema mode per model on HTTP error or invalid response
- 120-second hard timeout per generate call, matching the plan specification
- Shell wrapper uses `env "$@" ollama serve &` pattern for clean environment variable injection per profile

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Benchmark harness is ready for local execution with `bash scripts/ollama-bench.sh`
- Plan 02 (CI benchmark workflow) can reference these artifacts directly
- Plan 03 (run benchmarks, apply results) will use the JSON output from benchmark runs

## Self-Check: PASSED

All 6 created files verified present on disk. Both task commits (22777c3, e027616) verified in git log.

---
*Phase: 02.1-optimize-grader-model-selection*
*Completed: 2026-03-09*
