---
phase: 02.1-optimize-grader-model-selection
plan: 02
subsystem: infra
tags: [github-actions, ollama, benchmarking, ci, arm64]

# Dependency graph
requires:
  - phase: 02.1-optimize-grader-model-selection
    provides: benchmark harness script (tests/benchmark-grader.ts)
provides:
  - CI benchmark workflow for grader models on ARM64 runners
  - npm test:benchmark script for local invocation
affects: [02.1-optimize-grader-model-selection]

# Tech tracking
tech-stack:
  added: [ai-action/setup-ollama@v2, actions/cache@v5, actions/upload-artifact@v4]
  patterns: [Ollama lifecycle management with profile switching in CI, model caching for repeat runs]

key-files:
  created: [.github/workflows/benchmark-grader.yml]
  modified: [package.json]

key-decisions:
  - "60-minute timeout accounts for model pull time plus benchmark execution"
  - "OLLAMA_NUM_THREAD=4 hardcoded for CI 4-vCPU runners"

patterns-established:
  - "Ollama CI pattern: start default -> benchmark -> kill -> restart with env vars -> benchmark optimized"
  - "Model caching via actions/cache@v5 on ~/.ollama keyed by model list input"

requirements-completed: [SC-2, SC-4]

# Metrics
duration: 2min
completed: 2026-03-09
---

# Phase 2.1 Plan 02: CI Benchmark Workflow Summary

**GitHub Actions workflow for benchmarking grader models on ubuntu-24.04-arm runners with Ollama lifecycle management and three tuning profiles**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-09T11:38:10Z
- **Completed:** 2026-03-09T11:40:34Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- CI benchmark workflow ready for dispatch on feature branch
- Three tuning profiles (default, optimized-env, optimized-all) with proper Ollama restart cycle
- Model caching configured to speed up repeat benchmark runs
- Results uploaded as artifacts with 30-day retention for cross-platform comparison

## Task Commits

Each task was committed atomically:

1. **Task 1: Create CI benchmark workflow with Ollama setup and model caching** - `45cd61c` (feat)
2. **Task 2: Add benchmark npm script to package.json** - `799b533` (feat)

## Files Created/Modified
- `.github/workflows/benchmark-grader.yml` - CI benchmark workflow with workflow_dispatch/pull_request triggers, Ollama setup, model caching, three profile benchmark runs, artifact upload
- `package.json` - Added `test:benchmark` npm script

## Decisions Made
- 60-minute timeout: 72 benchmark runs x ~30s = ~36 min, plus model pull overhead
- OLLAMA_NUM_THREAD=4 hardcoded for CI runners (4 vCPU Cobalt 100), distinct from local 12-thread Snapdragon config

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- CI workflow can be dispatched via `gh workflow run benchmark-grader.yml --ref local-llm-grader` once pushed
- Results artifacts enable cross-platform comparison with local benchmark data
- Plan 03 can proceed with running benchmarks and analyzing results

## Self-Check: PASSED

- [x] `.github/workflows/benchmark-grader.yml` exists
- [x] `package.json` has `test:benchmark` script
- [x] Commit `45cd61c` found (Task 1)
- [x] Commit `799b533` found (Task 2)
- [x] `02.1-02-SUMMARY.md` created

---
*Phase: 02.1-optimize-grader-model-selection*
*Completed: 2026-03-09*
